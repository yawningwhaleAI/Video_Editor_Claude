[project]
name = "produce-engine"
version = "1.0.0"
dependencies = [
    "openai>=1.0.0",
    "rapidfuzz>=3.0.0",
]
requires-python = ">=3.10"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
